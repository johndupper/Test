<div class="row">
  <% @sources.each do |source| %>
        <div class="col-sm-6 col-md-4">
          <div class="thumbnail">
            <img class="image" src="<%= source.img_url %>" alt="<%= source.name %>">
            <div class="caption">
              <h3><%= source.name %></h3>
              <p>WHAT IS THIS</p>
              <p>
                <a href="#" class="btn btn-primary" role="button">Button</a>
                <a href="#" class="btn btn-default" role="button">Button</a>
              </p>
            </div>
          </div>
        </div>
  <% end %>
</div>

<div class="row">
  <% @sources.each do |source| %>
      <div class="col-sm-6 col-md-4">
        <div class="thumbnail">
          <img class="sources-img" src="<%= source.img_url %>" alt="">
          <div class="caption">
            <ul class="list-group">
              <!-- iterate headlines -->
              <% source.article.each do |article| %>
                  <li class="list-group-item">
                    <a href="<%= article["url"] %>" target="_blank">
                      <h4><%= article["title"] %></h4>
                    </a>
                    <p><%= article["description"] %></p>
                  </li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
  <% end %>
</div>
